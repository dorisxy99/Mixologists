<%- include('../partials/header') %>

<main id="drinks-index">
    <h2>Mixed Drinks!</h2>
    <section id="drinks">
        <a href="/drinks/new"><div>+ Add a new drink</div></a>
        <% drinks.forEach(function(r) { %>
            <a href="/drinks/<%= d._id %>"><div><%= d.name %></div></a>
            <% }); %>
    </section>
</main>

<%- include('../partials/footer') %>

<!-- <%- include('../partials/header') %>
<table id="list">
  <thead>
    <tr>
      <th>Title</th>
      <th>Release<br>Year</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% movies.forEach(function(m) { %>
      <tr>
        <td><%= m.title %></td>
        <td><%= m.releaseYear %></td>
        <td><a href="/movies/<%= m._id %>">DETAILS</a></td>
      </tr>
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %> -->